
var BuildingItem = cc.MenuItem.extend({
    //cc.ProgressTimer.create(sprite)
    //create a progress timer object with image 
    //file name that renders the inner sprite according to the percentage
    
    //setPercentage(percentage)
    
    
});


